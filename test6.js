const fs = require('fs');
const axios = require('axios');
const path = require('path');

// I can't require prisma, but I can read metadata or just query the db via API!
// wait, I can just query my API!
